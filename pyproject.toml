[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "open-agent-diary"
version = "0.1.0"
description = "Open Agent Diary: local-first inspectable memory and work-trace store for human/agent collaboration"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Willard Mechem" }
]
license = { text = "MIT" }
dependencies = []

[project.scripts]
agent-diary = "agent_diary.cli.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
